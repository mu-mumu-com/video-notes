# 秘書 週次レポート 2026-09-18

## プロジェクトごとの状況
### adult-directory
- 状況: 直近4週間で2コミット（09-11、いずれもnpm audit fix）。実装系の新規コミットはなし。放置ではなく保守は継続中。
- 目的達成度: 「1ページ・バナー表示型」（`page.tsx` / `AgeGate.tsx` / `banners.ts`）への作り直しは完了済みで、アフィリンク（Chaturbate）・広告バナー（adtng、20枠）とも実データが入っており空リンクやTODOは無し。一方、TASK.mdにある「mixhost（アダルトOK）へのホスティング移行＋`output:"export"`化」「ASP登録」は7月時点から未着手のまま進捗なし。README.mdも旧構成（`offers.ts`前提の多言語ディレクトリ型）の説明のままで、現行実装と乖離している（先々週から継続する指摘・未対応）。
- 提案: README.mdを現行実装に合わせて書き直す。mixhost移行が止まっているなら本人に状況を確認する。

### okinawa-navi（てだこナビ）
- 状況: 直近4週間で3コミット（脆弱性修正2件＋TASK.md更新）。最終コミットは09-11。制度データ・アクセシビリティ面の作り込みは継続的に進んでいる。
- 目的達成度: 制度データ（27サービス、7月に電話番号・組織名を公式ソースと照合済み）、WCAG AA相当のコントラスト改善など中身は充実。**しかし本番ドメイン`tedako-navi.net`は8/7頃から現在まで6週間以上ダウンしたまま**（原因＝レジストラ側のネームサーバー未設定、ユーザー作業待ちで9/11時点でも未解決とTASK.mdに記録あり）。Netlify側(`tedako-navi.netlify.app`)は正常。お問い合わせGoogleフォームもPLACEHOLDERのまま。「困っている人が支援窓口に繋がる」という目的の入口が塞がれた状態が続いている。
- 提案: ネームサーバー設定とGoogleフォーム発行、この2点が終わっているか本人に直接確認する（コードでは対応不可能な残タスクのため）。

### momemo
- 状況: 最終コミットは08-24で、以後3週間強コミットなし（3ヶ月未満のため棚卸し候補ではないが停滞気味）。
- 目的達成度: Netlify移行済み（`momemo-web.netlify.app`）で、projects.mdの本番URL表記とも一致（**先週指摘していたURL不一致は解消済み**）。Instagram（`@momemo_studio`）への導線はサイト内に実装済みで確認できた。一方、料金は全項目が仮数値、AI動画制作の実績は「準備中」のまま、専用LINE公式アカウントは未作成（別用途アカウントを暫定流用）、独自ドメインは意図的に保留——という3点が「本番品質」への残課題。ig-auto-reply側からmomemoへ戻す導線は本リポジトリ内には見当たらない。
- 提案: 料金の仮数値を正式化するタイミングを検討する（初受注が出てからで良いなら現状維持でOK）。

### サンプルサイト（shiro-clinic / kuruma-ya, K-sannpuru）
- 状況: main（shiro-clinic）・worktree-kuruma-ya-3d-portfolio（kuruma-ya）とも最終コミット09-11。両方とも活動継続中で放置なし。
- 目的達成度: shiro-clinicは主要対応が完了しVercel(`https://shiro-clinic.vercel.app`)で稼働中。kuruma-yaはthree.js / @react-three/fiber等を使った3Dスクロール演出（ドラッグ回転・ブルーム・キネティックタイポ）が実装済みで、直近もモバイル対応改善（61→79点）が入っている。開発者自身のログでは「地域の車屋サイトとしては89/100点、Awwwards級との比較では35〜45点」と自己評価しており、差別化訴求としては発展途上。
- 懸念: shiro-clinicは実際にVercelで公開済みなのに、projects.mdは「本番URL: 未確定」のまま（先週も指摘・未修正）。
- 提案: projects.mdの本番URL欄を実態に更新する。kuruma-yaは低ポリモデルのクオリティ底上げが次の一手（AI活用の提案(2)参照）。

### bento-order
- 状況: 直近4週間はnpm audit fix系コミットのみ（09-04, 09-11）。機能開発自体は06-28で止まっており、以後は保守コミットのみが続いている（3ヶ月未満のため棚卸し候補ではないが、次に動きがなければ格上げ候補）。
- 目的達成度: LINE連携弁当注文SaaSとしてPhase1〜7まで機能完結しており、ココナラ用サンプルとして提示できる状態を維持。サブスク化は保留のままで登録情報と一致し、動きの兆候もなし。README.mdはcreate-next-appの雛形のままで実装内容を反映しておらず、サンプル提示時には差し替えが必要。package.jsonにStripe関連依存が残るが未使用の可能性あり（サブスク保留中なら整理候補）。
- 提案: README.mdを実装内容（LINE弁当注文SaaS）に合わせて書き直す。

### bizplan-app
- 状況: 直近4週間はnpm audit fix系コミット2件のみ（09-11）。機能追加系の動きはなし。
- 目的達成度: 9ステップ入力→PDF出力というコア体験（汎用・不動産の2種類）は実装済みだが、**「AIで事業計画書を作成する」という目的の核心部分について、コード上にAI/LLM API連携が一件も見当たらない**（フォーム入力→テンプレート出力のみ）。販売に向けては独立LP・価格モデル・決済導線（Stripe等）・アカウント管理のいずれも未着手で、現状は「無料の社内ツール」の域を出ていない。README.mdもcreate-next-appの雛形のまま。
- 提案: 下記AI活用の提案(1)のとおり、Claude APIで実際に計画書ドラフトを生成する最小機能をまず1本実装し、「AIで作成」という謳い文句と実態を合わせるところから着手する。

### ig-auto-reply
- 状況: 最終コミットは09-11（脆弱性修正のみ）。機能実装自体は06-28のデプロイ完了以降、目立った進展なし（約2.5ヶ月停滞、3ヶ月未満のため棚卸し候補ではないが要注意）。
- 目的達成度: Cloudflare Workers上に本番デプロイ済み（`ig-auto-reply.9nago-nago9.workers.dev`）、Webhook署名検証・24時間重複排除・自己ループ防止など基盤は堅牢。返信処理はすべてMetaからの受信Webhookをトリガーに実行される設計で、能動的な一括送信ロジックは無く、**規約（reactive-only）の範囲は守られている**。一方、返信文面は疎通確認用の仮固定文のままで、momemo/ココナラへの導線（リンク・CTA）は未実装。Metaアプリ審査も未完了で一般ユーザーには未提供。
- 懸念: Webhook受信ペイロード全文を`console.log`しており、DM本文等の個人情報がログに残っている（開発者自身がTODOで認識済み）。
- 提案: 下記AI活用の提案(3)参照。あわせてログ出力の削減とMeta側審査の進捗を本人に確認する。

## 依存パッケージの脆弱性（自動修正結果）
今週は package.json のある全7対象（adult-directory / okinawa-navi / bento-order / bizplan-app / ig-auto-reply / shiro-clinic / kuruma-ya）で `npm install` → `npm audit` を実行したが、**いずれも high/critical は0件**（直近1〜2週間以内のコミットで既に解消済みだったため）。kuruma-yaのみ moderate 2件（vitest内部の`@vitest/mocker`、devDependency）が残るが、ルール上high/critical未満は対応対象外のため見送り。

- 修正したもの: 今週は無し（全対象で追加のfixが不要と判定）
- 修正見送り: 無し（ビルド/テスト失敗によるものは無し）
- 対象外: momemo（package.jsonなし、静的HTMLサイト）
- 今週は修正対象なし

## 棚卸し候補（3ヶ月以上動きなし）
特になし。全対象プロジェクトで直近1〜4週間以内にコミットがある。ただしbento-order・ig-auto-reply・bizplan-appは機能開発自体が2〜2.5ヶ月止まっており、依存修正コミットのみで動きが継続している状態。今後も機能面の進捗がないまま推移すると、数ヶ月以内に棚卸し候補へ格上げする可能性がある。

## AI活用の提案
- **bizplan-appにClaude APIでのAI生成を実装し、プロンプトキャッシングでコストと一貫性を両立する**: 現状「AIで作成」と謳いながらAI連携が無い状態なので、まず銀行融資向けの書式・審査観点をまとめた長いシステムプロンプト（フォーマット定義）をprompt cachingで固定し、ユーザー入力部分だけ都度差し替えて計画書ドラフトを生成する最小実装から始めると、複数ユーザーが使っても再読み込みコストを抑えつつ出力の一貫性を保ちやすい。
- **K-sannpuruのサンプルサイトをWorkflowツールで並列量産する**: ココナラ営業用に業種別デモ（美容クリニック=shiro-clinic、車屋=kuruma-yaの他）を増やしたい場合、既存コンポーネントをベースに複数の業種向けデモを並行生成するWorkflowを組むと、1つずつ手作業で作るより短時間でポートフォリオの幅を広げられる。
- **この秘書業務自体を固定フェーズのWorkflowスクリプトとして定型化する**: 今回は7リポジトリの点検・audit修正を並行エージェントに手動で委任したが、これは「棚卸し→audit修正→ビルド検証→レポート生成」という固定パイプラインそのもの。毎週同じ組み立てを繰り返す代わりにWorkflowスクリプト化すれば、抜け漏れが減り再現性も上がる。

## 動画メモから使えそうなもの
今週も新着メモなし（video-notesリポジトリ内の最新メモは引き続き7/30〜8/5分のまま、6週間以上新規追加が無い状態。先週も同様の指摘あり）。既存メモから、今回の点検結果に直結するものを2つ:
- **Claude CodeとSplineで作る3Dウェブサイト**（AIツール/2026-07-30）: kuruma-yaは開発者自身が「Awwwards級には届いていない（低ポリモデルのまま）」と自己評価している。Splineのプロ級3Dテンプレートをベースモデルの底上げに使えば、実装の作り込み（演出ロジック）はそのままに見た目のクオリティを上げられる可能性がある。
- **インスタで伸びない本当の理由と対策**（SNS運用/2026-08-01）: 「投稿数よりコメント・DM対応の質」が要点。ig-auto-replyの返信文面が仮固定文のままという今回の指摘、momemoのIG運用の両方に直結する示唆。

## 気になっていること
- okinawa-naviの本番ドメイン`tedako-navi.net`が8/7頃から6週間以上ダウンしたまま（レジストラでのネームサーバー変更というユーザー作業待ちが継続。9/11時点のTASK.mdでも未解決と確認済み）。「困っている人が支援窓口に繋がる」という目的の入口が塞がれている点は特に優先度が高い。
- shiro-clinicは実際にVercelで公開済み（`https://shiro-clinic.vercel.app`）なのに、projects.mdは「本番URL: 未確定」のまま（先週も指摘・未修正）。kuruma-yaも先週時点でVercel公開が確認されているため、同様に食い違ったままの可能性が高い（今週は個別確認までは行っていない）。
- bizplan-appは「実装済み・販売検証前」というprojects.mdの表記に対し、目的の核心であるAI生成機能が実態としてまだ無い（フォーム入力＋PDF出力のみ）。表記が実態よりやや前向き。
- adult-directoryのREADME.mdが現行実装（`banners.ts`ベースの1ページ構成）と食い違う旧説明（`offers.ts`前提の多言語ディレクトリ型）のまま、複数週にわたり未対応。
- ig-auto-replyがWebhook受信ペイロード全文を`console.log`しており、DM本文等の個人情報がログに残る設計のまま（開発者自身がTODOで認識済み）。
- video-notesの動画メモ（AIツール/SNS運用/マネジメント/自己啓発）が7/30〜8/5分を最後に6週間以上新規追加されていない。先週のレポートでも同様の指摘をしていたが、依然として解消していない。
- 3d-mindmapはprojects.md記載のプロジェクトだが、このクラウド実行環境にはリポジトリが存在しない（ローカル`~/開発/3d-mindmap`限定）ため、今週も点検対象から除外。ローカル秘書呼び出し時に別途確認が必要。
